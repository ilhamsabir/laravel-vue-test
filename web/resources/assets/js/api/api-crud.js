import axios from 'axios'
import {
	baseUrl,
	CSRF_TOKEN,
} from '../utils/library.js'

const BASEAPI = baseUrl

export const requestDataApi = () => {

	// const url = `${BASEAPI}/api/analytics/${id}`

	// const config = {
	// 	url,
	// 	method: 'get',
	// 	headers: {
	// 		'X-CSRF-TOKEN': CSRF_TOKEN,
	// 	},
	// }

	// return axios(config)

	return []
}